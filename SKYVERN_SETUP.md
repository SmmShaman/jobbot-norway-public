# 🤖 JobBot - Skyvern Integration Setup Guide

## ✅ Що ми реалізували

Тепер JobBot використовує **Skyvern** для реального сканування вакансій (як людина), замість простого HTML scraping.

### 📊 Архітектура (Варіант 3 - Гібрид):

```
┌─────────────────────────────────────────────────────────┐
│ 1. Dashboard (браузер)                                  │
│    https://jobbotnetlify.netlify.app                   │
│    Користувач додає URL для сканування                 │
└────────────┬────────────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────────────┐
│ 2. Backend API (Google Cloud Run)                       │
│    Створює запис в таблиці scan_tasks                  │
│    Status: PENDING                                      │
└────────────┬────────────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────────────┐
│ 3. Supabase Database (інтернет)                        │
│    Таблиця: scan_tasks (черга завдань)                │
│    ┌──────────┬───────────────┬──────────┐            │
│    │ user_id  │ url           │ status   │            │
│    ├──────────┼───────────────┼──────────┤            │
│    │ bd95...  │ finn.no/...   │ PENDING  │            │
│    └──────────┴───────────────┴──────────┘            │
└────────────┬────────────────────────────────────────────┘
             ↑
             │ (перевіряє кожні 10 секунд)
┌─────────────────────────────────────────────────────────┐
│ 4. Worker (ТВІЙ ПК) - Python скрипт                    │
│    - Читає PENDING tasks з Supabase                    │
│    - Викликає Skyvern API (localhost:8000)             │
│    - Зберігає результати в Supabase                    │
└────────────┬────────────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────────────┐
│ 5. Skyvern (ТВІЙ ПК)                                    │
│    http://localhost:8000                                │
│    - Відкриває браузер (Playwright)                     │
│    - AI аналізує сторінку (GPT-4V)                      │
│    - Витягує дані вакансій                              │
└────────────┬────────────────────────────────────────────┘
             ↓
┌─────────────────────────────────────────────────────────┐
│ 6. Результати → Supabase → Dashboard                   │
│    Таблиця: jobs                                        │
│    Dashboard показує всі відскановані вакансії         │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Крок 1: Створи таблицю scan_tasks в Supabase

### 1.1 Відкрий Supabase SQL Editor

Перейди: https://app.supabase.com/project/ptrmidlhfdbybxmyovtm/sql

### 1.2 Виконай SQL скрипт

**GitHub лінк:**
```
https://raw.githubusercontent.com/SmmShaman/jobbot-norway-public/claude/netlify-ui-011CUqJXNw4wkoYPis8TAkxF/scan_tasks_schema.sql
```

Або скопіюй вміст файлу `scan_tasks_schema.sql` і виконай в SQL Editor.

**Що створить:**
- ✅ Таблиця `scan_tasks` (черга завдань для Worker)
- ✅ Індекси для швидкості
- ✅ RLS policies (безпека)
- ✅ Додаткові поля в таблицю `jobs`

---

## 🖥️ Крок 2: Встанови Worker на своєму ПК

### 2.1 Переконайся що Skyvern працює

```bash
curl http://localhost:8000/api/v1/health
```

Якщо Skyvern не працює:
```bash
cd /path/to/skyvern
docker-compose up -d
```

### 2.2 Встанови Python залежності

```bash
cd worker
pip install -r requirements.txt
```

### 2.3 Створи .env файл

```bash
cp .env.example .env
```

Відкрий `.env` і додай свої ключі:

```env
SUPABASE_URL=https://ptrmidlhfdbybxmyovtm.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGci...  # service_role key
SKYVERN_API_URL=http://localhost:8000
```

**Де взяти SUPABASE_SERVICE_KEY?**
1. Перейди: https://app.supabase.com/project/ptrmidlhfdbybxmyovtm/settings/api
2. Скопіюй **service_role** key (секретний ключ)

### 2.4 Запусти Worker

```bash
python worker.py
```

Ти побачиш:
```
🤖 JobBot Worker Started
🆔 Worker ID: worker-abc12345
⏱️ Poll Interval: 10s
===================================

💤 No pending tasks. Waiting 10s...
```

---

## ☁️ Крок 3: Deploy нового Backend

Backend вже оновлений і закомічений в Git. Потрібен новий deployment.

### 3.1 Тригерни GitHub Actions

Зміни вже запушені в гілку `claude/netlify-ui-011CUqJXNw4wkoYPis8TAkxF`.

GitHub Actions автоматично задеплоїть новий backend з оновленим API.

Перевір статус: https://github.com/SmmShaman/jobbot-norway-public/actions

### 3.2 Дочекайся успішного deployment

Зачекай ~5 хвилин поки deployment завершиться.

---

## 🧪 Крок 4: Тестування

### 4.1 Додай Search URLs в Settings

1. Відкрий: https://jobbotnetlify.netlify.app/settings
2. Додай тестовий URL:

**FINN.no приклад:**
```
https://www.finn.no/job/fulltime/search.html?occupation=0.23&location=0.20061
```

**NAV.no приклад:**
```
https://arbeidsplassen.nav.no/stillinger?q=developer&counties=OSLO
```

3. Натисни "Save Settings"

### 4.2 Запусти сканування

1. Відкрий: https://jobbotnetlify.netlify.app/dashboard
2. Натисни кнопку **"Scan Jobs Now"**
3. Ти побачиш:
   ```
   Created 1 scan tasks. Worker will process them shortly.
   ```

### 4.3 Дивись логи Worker

У терміналі де запущений Worker ти побачиш:

```
📋 Processing task: 5694561a...
🌐 Source: FINN
🔗 URL: https://www.finn.no/job/fulltime/search.html...
============================================

🤖 Calling Skyvern API: http://localhost:8000
📄 Template: FINN, URL: https://www.finn.no/...
✅ Skyvern task created: task-xyz123
⏳ Skyvern task running: task-xyz123
✅ Skyvern task completed: task-xyz123
📊 Found 25 jobs

🔍 Extracting details for job 1/25: Python Developer...
✅ Got detailed data for: Python Developer at Equinor
💾 Saved job: Python Developer at Equinor

✅ Task completed: 25/25 jobs saved
```

### 4.4 Переглянь результати в Dashboard

1. Оновіть Dashboard
2. Перейди на сторінку **Jobs** (коли буде готова UI)
3. Ти побачиш всі відскановані вакансії з повними деталями:
   - Назва вакансії
   - Компанія
   - Локація
   - Повний опис
   - Контактні дані
   - Вимоги
   - Benefits
   - Дедлайн

---

## 📊 Моніторинг

### Переглянути всі scan tasks в Supabase:

```sql
SELECT id, user_id, source, status, jobs_found, created_at
FROM scan_tasks
ORDER BY created_at DESC
LIMIT 10;
```

### Переглянути відскановані вакансії:

```sql
SELECT title, company, location, source, status, discovered_at
FROM jobs
WHERE user_id = 'bd952edf-8468-48fb-a02f-a1a113358cd0'
ORDER BY discovered_at DESC
LIMIT 20;
```

### Перевірити Worker логи:

```bash
tail -f worker/worker.log
```

---

## 🛠️ Troubleshooting

### Worker не знаходить завдань:

```
💤 No pending tasks. Waiting 10s...
```

**Перевір:**
1. Чи додав ти URLs в Settings?
2. Чи натиснув "Scan Jobs Now" в Dashboard?
3. Чи є записи в `scan_tasks` таблиці (Supabase SQL):
   ```sql
   SELECT * FROM scan_tasks WHERE status = 'PENDING';
   ```

### Skyvern не працює:

```
❌ Cannot connect to Skyvern at http://localhost:8000
```

**Рішення:**
```bash
docker ps | grep skyvern  # Перевір чи працює
docker-compose up -d skyvern  # Запусти якщо не працює
```

### Backend не створює tasks:

**Перевір чи новий код задеплоєний:**
1. GitHub Actions: https://github.com/SmmShaman/jobbot-norway-public/actions
2. Переконайся що останній deployment успішний

**Тест API напряму:**
```bash
curl -X POST "https://jobbot-backend-831800378689.europe-west1.run.app/api/scan-jobs" \
  -H "Content-Type: application/json" \
  -d '{"user_id":"bd952edf-8468-48fb-a02f-a1a113358cd0","scan_type":"MANUAL"}'
```

---

## ✅ Що далі?

1. **AI Аналіз релевантності** - після сканування, AI аналізуватиме чи вакансія підходить
2. **Автоматичне заповнення форм** - Skyvern заповнюватиме application forms
3. **Dashboard UI для Jobs** - красива таблиця з вакансіями
4. **Фільтри та пошук** - фільтрувати за компанією, локацією, релевантністю

---

## 📝 Корисні команди

### Запустити Worker у фоні (Linux/Mac):
```bash
nohup python worker/worker.py > worker_output.log 2>&1 &
```

### Зупинити Worker:
```bash
ps aux | grep worker.py
kill <PID>
```

### Переглянути статус Skyvern:
```bash
docker ps | grep skyvern
docker logs skyvern-skyvern-1 --tail 50
```

### Очистити failed tasks:
```sql
DELETE FROM scan_tasks WHERE status = 'FAILED';
```

---

**Готово! Система сканування через Skyvern налаштована!** 🎉

Тепер коли користувач натискає "Scan Jobs", Worker автоматично сканує сайти через Skyvern і зберігає всі деталі вакансій в базу! 🚀
